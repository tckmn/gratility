to build web version:
  make
  firefox dist/gratility.html

to build command line version:
  npm i
  tsc -p cmdline.json
  ./gratility --help

to run server:
  npm i
  tsc -p cmdline.json
  mkdir serverdata
  echo 1234 > serverdata/port
  ./gratility serve


==


steps to add a new tool:
- implement it in src/tools/*.ts
- add it to src/tools/alltools.ts
- make a gui for it in Toolboxbox#generateMenu in src/toolbox.ts
- optional: add a default bind in src/toolbox.ts
